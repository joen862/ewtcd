<?php
namespace App\Http\Controllers;

use App\Models\Wallet;
use App\Models\Market;
use App\Models\Supply;
use App\Models\Bridged;

class UpdateController extends Controller {

    public function update() {

        /*$market = new Market;
        if($market->updateMarketData()) {
            dump('Market data updated');
        }

        $bridged = new Bridged;
        if($bridged->updateBridgedData()) {
            dump('EWTB balances updated');
        }

        $supply = new Supply;
        if($supply->updateSupplyData()) {
            dump('Supply data updated');
        }

        $wallet = new Wallet;
        if($wallet->updateBalances()) {
            dump('Wallets updated');
        }

        dump('Done! Everything is updated');*/

    }

}
